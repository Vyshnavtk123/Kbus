services:
  - type: web
    name: kbus
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn kbus_project.wsgi:application --bind 0.0.0.0:$PORT
    autoDeploy: true
